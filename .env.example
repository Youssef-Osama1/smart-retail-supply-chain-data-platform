# Copy to .env for local (non-Docker) runs. When using docker-compose the
# DATABASE_URL is injected automatically and points at the `retail` database.
DATABASE_URL=postgresql+psycopg2://airflow:airflow@localhost:5432/retail
